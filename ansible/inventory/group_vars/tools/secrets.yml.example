# ─── AdGuard Home Admin ───────────────────────────────
# Hash bcrypt de la contraseña del panel de administración de AdGuard Home.
# Generá tu hash con: htpasswd -nBC 10 "" | tr -d ':\n'
adguard_admin_password_hash: "$2y$10$CHANGE_ME_WITH_HTPASSWD_HASH"
