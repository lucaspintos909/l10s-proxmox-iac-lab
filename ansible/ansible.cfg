[defaults]
inventory = inventory/hosts.yml
roles_path = roles
remote_user = ubuntu
private_key_file = ~/.ssh/proxmox_lab
host_key_checking = false
retry_files_enabled = false
stdout_callback = yaml
callbacks_enabled = timer

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
