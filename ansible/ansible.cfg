[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = False
remote_user = lpintos
private_key_file = ~/.ssh/tailscale

[privilege_escalation]
become = True
become_method = sudo
